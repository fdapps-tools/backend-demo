{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node -r dotenv/config ./bin/server",
    "dev": "nodemon -r dotenv/config ./bin/server --ignore __tests__",
    "test": "jest",
    "test:watch": "NODE_ENV=test jest --watch"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "dotenv": "^10.0.0",
    "express": "~4.16.1",
    "folder-hash": "^4.0.1",
    "morgan": "~1.9.1",
    "node-cron": "^3.0.0",
    "tunneling": "git+https://github.com/fdapps-tools/tunneling.git"
  },
  "devDependencies": {
    "jest": "^27.0.6",
    "nodemon": "^2.0.12"
  }
}
